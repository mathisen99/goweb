//this is the only thing we need to change when we go live with the app
//=================================================================================================
//=====================================  CONTENT FOR LAUNCH  ======================================
/* fmt.Fprintln(os.Stdout, "Server started successfully at port 443 (https) and 80 (http) for launch URL: https://www.mathizen.org")
err := http.ListenAndServeTLS(":443", "certs/fullchain7.crt", "certs/privkey7.key", nil)
if err != nil {
    fmt.Fprintln(os.Stderr, "Server failed to start")
    fmt.Fprintln(os.Stderr, err)
} */
//=================================================================================================